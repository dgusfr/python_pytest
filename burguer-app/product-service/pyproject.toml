[project]
name = "product-service"
version = "0.1.0"
description = "Product Service"
requires-python = ">=3.13"
dependencies = [
    "Flask",
    "pymongo",
    "python-dotenv",
    "werkzeug",
    "pytest>=8.4.1",
    "pytest-cov>=6.2.1",
]
